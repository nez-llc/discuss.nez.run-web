import * as twitter from 'auth/providers/twitter/server'
import * as facebook from 'auth/providers/facebook/server'
import * as github from 'auth/providers/github/server'

export const providers = {
  facebook,
  github,
  twitter,
}
